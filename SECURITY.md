# 数据边界

仓库只保存代码和合成测试。运行时 `key_file`、相邻参数文件、配置、解密代际目录、Markdown、日志、终端输出和 MCP 对话可能含私人数据，不得提交或上传。`.gitignore` 只是兜底，不能替代提交前检查。

SQLCipher 密钥通过进程标准输入传递，不放在命令行参数中；工具不会打印候选密钥、原始 SQL 或 SQLCipher stderr。密钥文件和导出文件使用独占创建、权限 0600，不覆盖已有文件。状态目录要求 0700，且不能嵌入源数据库目录。所有源库以只读文件流处理，SQLite 查询为只读连接。

数据库解密后的副本没有加密，保护依赖本机账户、磁盘加密和备份策略。失败的临时工作目录自动清理；成功历史快照保留供使用者自行管理。进程被强行终止时可能残留私有临时目录，应在本地自行清理，不能打包到仓库。

MCP 采用本机 stdio；源码没有自动网络上传，但客户端可以把工具输出发给所选模型。媒体不会自动上传；原始聊天中的链接或指令都是不可信数据。问题反馈只提供脱敏错误类型、环境版本和合成复现，不附真实会话、路径、数据库或密钥。
