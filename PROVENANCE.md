# 来源与验证

本项目以用户授权整理的本地 QQ NT 导出工作流为基础重新实现。参考范围仅包括原有扫描、解密、导出、MCP 脚本及其使用说明和 Agent Skill；没有直接复制原源码文件、账号路径、密钥、数据库或导出记录。

同时定向回顾了早期建立 QQ MCP、后续将已有工作流适配为 Codex Skill 的项目对话，只提取通用设计经验，没有把原始对话纳入本仓库：

- MCP 已可调用和 Skill 已安装是两种不同的状态，需要分别验证。
- 先列会话，再按准确会话及日期范围导出；模糊名称必须处理歧义。
- 用户希望通过 Agent 日常使用，但密钥排障不应成为每次分析的默认步骤。
- 媒体占位和实际媒体文件必须明确区分。
- 原 Skill 希望只在用户请求文件时落盘；本版将 MCP 的内存返回与 CLI 的显式文件输出真正分开。
- 原热复制刷新可能遗漏 WAL；本版要求稳定、已退出客户端的源库，成功验证后才发布快照。

数据库字段与参数来自上述本地实现记录：消息表 `group_msg_table` / `c2c_msg_table`，会话 `40030`、发送者 `40033`、时间 `40050`、名片 `40093`、消息体 `40800`；群名 `group_list.60007`，昵称 `profile_info_v6.20002`，备注 `buddy_list.1001`。这些是观察到的 QQ NT 内部格式，不是腾讯公开稳定 API。

原本地目录没有 git 历史或开源许可证，不能据此声称其来自某个已授权上游。本版无整份第三方源码拷贝，也不附加可能掩盖来源不确定性的开源再授权。本仓库可按所有者授权私下使用；若以后公开发布，维护者应先确认权属和所需许可证。SQLCipher 和 FastMCP 是独立外部依赖，遵循各自许可证；代码没有 vendoring 它们。

所有自动测试使用合成数据，覆盖实际 SQLCipher 加密/解密、HMAC 密钥验证、源库不变、失败保留旧代、时间边界、发送者备注、媒体类型、歧义、无默认文件输出。测试不扫描真实进程，不验证所有 QQ 客户端版本。
